# ── Simzzy Chatbot Frontend — Environment ─────────────────────────────────
# Set this to your deployed backend URL in production.
# In development, Vite's proxy (vite.config.js) handles /api automatically.

VITE_CHATBOT_API=http://localhost:3001/api/chat
